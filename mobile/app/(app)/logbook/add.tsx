// Add Flight (new) — multi-leg duty builder. See src/components/FlightForm.
import FlightForm from '../../../src/components/FlightForm';

export default function AddFlight() {
  return <FlightForm mode="new" title="Log a Flight" />;
}
